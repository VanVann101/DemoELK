# ELK + .NET demo

Демо стенд для аттестации: три минимальных микросервиса и стек Elasticsearch/Kibana/Logstash в одном `docker-compose`.

## Сервисы
- `order-api` — принимает заказы, ходит в `inventory-service` и `payment-service`, пишет логи.
- `inventory-service` — проверяет наличие товара, иногда отвечает 500/долгие ответы.
- `payment-service` — имитирует оплату: успех / недостаточно средств / 500.
- `filebeat` — собирает логи из файлов сервисов и отправляет в Logstash.
- `logstash` — принимает логи от Filebeat (порт 5044), нормализует и кладёт в Elasticsearch.
- `elasticsearch`, `kibana` — хранение и визуализация логов.
- `locust` — генератор нагрузки для создания тысяч логов (Web UI на порту 8089).

## Запуск
```bash
docker compose up --build
```
- Order API: http://localhost:8080
- **Swagger UI**: http://localhost:8080/swagger (интерактивная документация API)
- **Locust UI**: http://localhost:8089 (генератор нагрузки для тестирования)
- Kibana: http://localhost:5601 (security выключен для простоты)
- Logstash Beats input: tcp://localhost:5044
- Elasticsearch: http://localhost:9200

Остановить: `docker compose down -v` (удалит данные ES и логи).

## Управление временем жизни логов

Проект использует **Index Lifecycle Management (ILM)** для автоматического управления логами.

### Как это работает:

1. **Logstash** создаёт индексы по датам: `dotnet-logs-2026.01.27`, `dotnet-logs-2026.01.28`, ...
2. **ILM политика** автоматически управляет фазами:
   - **Hot** (0-3 дня): активные логи, высокий приоритет восстановления
   - **Cold** (3-7 дней): старые логи, низкий приоритет
   - **Delete** (7+ дней): автоматическое удаление

### Автоматическая настройка:

ILM политика **настраивается автоматически** при первом запуске через init-контейнер `elasticsearch-init`.

```bash
docker compose up -d
```

Политика применится автоматически, никаких дополнительных действий не требуется!

### Ручная настройка (опционально):

Если нужно изменить политику вручную или применить к существующим индексам:

```powershell
.\setup-ilm.ps1
```

### Изменение срока хранения:

Отредактируйте `init-ilm.sh` или `setup-ilm.ps1`, измените:
```bash
"min_age": "7d"  # на нужное значение, например "30d"
```

Затем пересоздайте контейнеры:
```bash
docker compose down
docker compose up -d
```

### Проверка статуса:

```powershell
# Список индексов с размером
Invoke-RestMethod 'http://localhost:9200/_cat/indices/dotnet-logs-*?v'

# Статус ILM для каждого индекса
Invoke-RestMethod 'http://localhost:9200/dotnet-logs-*/_ilm/explain' | ConvertTo-Json -Depth 10

# Или используйте удобный скрипт
.\check-indices.ps1
```

## Проверка сценария

### Через Locust (рекомендуется для генерации нагрузки)
Откройте http://localhost:8089 и настройте:
1. **Number of users**: 50 (одновременных пользователей)
2. **Spawn rate**: 5 (новых пользователей в секунду)
3. Нажмите **"Start swarming"**

Locust автоматически создаст тысячи запросов с реалистичным распределением сценариев:
- 50% успешных заказов
- 15% out of stock
- 10% insufficient funds
- 10% ошибки сервисов
- 15% медленные запросы

**Подробная документация:** см. [LOAD_TESTING.md](LOAD_TESTING.md)

### Через Swagger UI (для ручного тестирования)
Откройте http://localhost:8080/swagger и используйте интерактивный интерфейс:
1. Раскройте `POST /orders`
2. Нажмите "Try it out"
3. Измените `itemId` на нужный сценарий (1-7, см. таблицу выше)
4. Нажмите "Execute"
5. В ответе получите `traceId` для отслеживания в Kibana

**Подробное описание всех сценариев:** см. [TEST_SCENARIOS.md](TEST_SCENARIOS.md)

### Через curl
Отправить заказ:
```bash
curl -X POST http://localhost:8080/orders ^
  -H "Content-Type: application/json" ^
  -d "{\"itemId\":1,\"quantity\":2,\"userId\":\"demo-user\"}"
```
Ответ содержит `traceId` (для наглядности), логи — в Kibana Discover.

Просмотр конкретного заказа:
```bash
curl http://localhost:8080/orders/{orderId}
```

## Что смотреть в Kibana
- **Discover**: индекс `dotnet-logs-*` — нормализованные Logstashом логи из разных форматов:
  - **order-api**: JSON формат
  - **inventory-service**: Key/Value текстовый формат
  - **payment-service**: Logfmt формат
  
Все форматы парсятся Logstash и приводятся к единой структуре с полями: `service`, `level`, `timestamp`, `traceId`, `msg`.

**Фильтрация в Kibana:**
- По сервису: `service:"order-api"`
- По traceId: `traceId:"abc123"`
- По уровню: `level:"error"`
- Исключить ошибки парсинга: `NOT tags:_jsonparsefailure`

**Примечание:** Логи с тегом `_jsonparsefailure` — это сообщения, которые не удалось распарсить как JSON (обычно системные логи .NET или пустые строки). Они сохраняются как plain text с `service:"unknown-service"`.

## Архитектура логирования
Проект использует **Filebeat** для сбора логов:
1. Сервисы пишут логи в файлы `/app/logs/*.log`
2. Filebeat читает файлы и отправляет в Logstash (порт 5044)
3. Logstash парсит разные форматы и отправляет в Elasticsearch
4. Kibana визуализирует логи

**Разделение ответственности:**
- **Сервисы (.NET)**: пишут структурированные логи в файлы через Serilog
- **Filebeat**: легковесный shipper, читает файлы и отправляет строки в Logstash
- **Logstash**: парсит все форматы (JSON, Key/Value, Logfmt), нормализует и обогащает данные
- **Elasticsearch**: хранит нормализованные логи
- **Kibana**: поиск и визуализация

## Форматы логов
Проект демонстрирует работу с тремя разными форматами логов:
1. **order-api**: JSON (полный структурированный формат Serilog)
   - Все свойства логируются как отдельные поля
   - `TraceId`, `Level`, `Timestamp` и другие метаданные
   - **Filebeat парсит JSON** с `json.keys_under_root: true`
2. **inventory-service**: Key/Value текстовый формат (custom template)
   - Формат: `inventory | timestamp | level=X | item=Y | traceId=Z | msg=...`
   - **Filebeat отправляет как plain text**, парсинг в Logstash через grok
3. **payment-service**: Logfmt формат (key="value" pairs)
   - Формат: `timestamp="..." level=X service=Y traceId="Z" message="..."`
   - **Filebeat отправляет как plain text**, парсинг в Logstash через grok

Logstash нормализует все три формата в единую структуру для Elasticsearch.

**Важно:** В JSON логах (order-api) параметры логирования (например, `{TraceId}`, `{@Order}`) сохраняются как отдельные поля в JSON, а не подставляются в текст сообщения. Это позволяет делать структурированный поиск по полям в Kibana.

## Распределённая трассировка (Distributed Tracing)
Проект реализует простую распределённую трассировку через HTTP заголовок `X-Trace-Id`:
1. **order-api** генерирует уникальный `traceId` для каждого запроса
2. Передаёт `traceId` в заголовках HTTP запросов к inventory-service и payment-service
3. Все три сервиса логируют один и тот же `traceId`
4. В Kibana можно найти все логи одного запроса по полю `traceId`

Пример поиска в Kibana: `traceId:"abc123def456"` покажет весь путь запроса через все сервисы.

## Поведение для демо
Проект использует **детерминированные тестовые сценарии** на основе `itemId`:

| itemId | Сценарий | Описание |
|--------|----------|----------|
| 1 | ✅ **Success** | Заказ успешно обработан |
| 2 | ❌ **Out of Stock** | Товара нет на складе (inventory) |
| 3 | 💳 **Insufficient Funds** | Недостаточно средств (payment) |
| 4 | 🔥 **Inventory Error** | Ошибка 500 в inventory service |
| 5 | 🔥 **Payment Error** | Ошибка 500 в payment service |
| 6 | 🐌 **Slow Inventory** | Задержка 2 сек в inventory service |
| 7 | 🐌 **Slow Payment** | Задержка 2 сек в payment service |

**Примеры запросов:**
```bash
# Успешный заказ
curl -X POST http://localhost:8080/orders -H "Content-Type: application/json" -d "{\"itemId\":1,\"quantity\":2,\"userId\":\"demo-user\"}"

# Нет на складе
curl -X POST http://localhost:8080/orders -H "Content-Type: application/json" -d "{\"itemId\":2,\"quantity\":2,\"userId\":\"demo-user\"}"

# Недостаточно средств
curl -X POST http://localhost:8080/orders -H "Content-Type: application/json" -d "{\"itemId\":3,\"quantity\":2,\"userId\":\"demo-user\"}"
```

